<%- include("partials/header", { activePage: "welcome" }) %>

<main class="page">
  <div class="container">
    <section class="card">

      <% if (user) { %>
        <h1 class="h1">Welcome, <%= user.name %> 👋</h1>
        <p class="p">You are registered for <strong>Chemistry Horizons (SSC2 2026)</strong>.</p>
      <% } else { %>
        <h1 class="h1">Welcome</h1>
        <p class="p">Session expired. Please login again.</p>
        <a class="btn btn-primary" href="/login">Go to Login</a>
      <% } %>

      <hr class="divider" />

      <% if (ratings) { %>
        <p class="p">✅ Thanks for rating the ceremony.</p>
      <% } else { %>
        <p class="p">You can rate the ceremony anytime.</p>
      <% } %>

      <div class="grid grid-4">
        <a class="btn btn-primary" href="/vote/poster">Vote: Best Poster</a>
        <a class="btn" href="/vote/speaker">Vote: Best Speaker</a>
        <a class="btn" href="/vote/organization">Rate Organization</a>
        <a class="btn" href="/results/poster">View Results</a>
      </div>

    </section>
  </div>
</main>

<%- include("partials/footer", { footerTag: "SSC2 2026" }) %>
